# Cloudflare Pages configuration.
#
# Build output directory is set to static/.
# D1 database bindings are configured in the Cloudflare Dashboard:
#   Pages Project -> Settings -> Functions -> D1 database bindings:
#   Variable name: DB -> select your D1 database
name = "pocketsly"
compatibility_date = "2025-06-01"
pages_build_output_dir = "static"
