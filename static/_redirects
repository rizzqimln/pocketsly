# SPA fallback: every unknown path serves the app shell (index.html).
/*    /index.html   200

# Optional: proxy the Python backend (Setup B in README.md). The frontend
# already calls relative /api/* paths, so this one line makes Netlify forward
# them to your backend host — no CORS changes needed and HttpOnly session
# cookies keep working. Uncomment and replace the URL after deploying the
# backend, and keep it ABOVE the /* rule:
#
# /api/*  https://your-backend.onrender.com/api/:splat  200
