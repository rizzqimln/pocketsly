# Cloudflare Pages + Netlify-compatible routing.
#
# /api/* is intentionally NOT listed here. On Cloudflare Pages it is handled
# directly by the edge functions at functions/api/[[path]].js (D1-backed,
# canonical production backend) — no external backend is proxied.
#
# Netlify alternative (only if you ever deploy the API there again): uncomment
# this line ABOVE the SPA fallback and point it at your backend:
# /api/*  https://<your-backend>/api/:splat  200

# SPA fallback: every unknown path serves the app shell (index.html).
/*    /index.html   200